import styled from "styled-components";

export const ProfileNameStyled = styled.h2`
font-size:${props=>props.font || "16px"};
color:${props=>props.color || "black"};
padding:${props=>props.padding || "0"}`;